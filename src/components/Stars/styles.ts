import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        justifyContent: 'flex-end',
        alignItems: 'center',
    },
    canvas: {
        height: 257,
        width: 249,
        position: 'absolute',
        zIndex: 1,
    }
})